[project]
name = "anduril-ops"
version = "0.1.0"
description = "ISR Drone Deployment Trade Study"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "pandas>=2.0",
  "pyyaml>=6.0",
]

[tool.setuptools.packages.find]
where = ["src"]
